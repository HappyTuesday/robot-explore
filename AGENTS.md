# Robot Explore 开发与设计约定

本文件适用于整个仓库。新增游戏、修改现有界面时必须遵守；用户明确提出的任务要求优先。

## 产品定位与技术基础

- 面向儿童的机器人探索、益智与闯关小游戏站点。操作简单、反馈明确、鼓励尝试，不用复杂说明才能开始玩。
- 使用 React + TypeScript + Vite。每个小游戏有独立页面和路由，共享平台导航、主题、角色、弹窗和音效能力。
- 以第一款“彩虹能量迷宫”为视觉基准，但不要复制它的具体规则到所有新游戏。海洋、太空等世界可以有自己的主题配色和场景。
- 修改前检查当前代码和工作区差异；保留其他任务正在进行的改动，不顺手重构或覆盖无关文件。

## 视觉风格

- 整体采用明亮、温和、精致的儿童绘本风格：浅米白背景、薄荷绿主色、深绿文字、圆角面板、轻阴影，以及少量柔和的紫色、橙色和金色。
- 优先复用 `src/styles.css` 的主题变量与 `src/components/Artwork.tsx` 的插画语言。新增主题通过局部变量和组件扩展，不给每个游戏建立互不相干的设计系统。
- 界面首先应当像一个可以游玩的游戏。不要把游戏页做成卡片密集的后台仪表盘，也不要用大段说明、大标题或装饰挤占游戏区。
- 角色必须在小尺寸下也能区分，不能只依赖颜色：机器人保留方形机械头、天线和机械肢体；怪兽使用明显不同的轮廓、眼睛或角等特征。颜色、图标和简短标签共同表达身份与状态。
- 机器人所在格不显示格子的边框、外轮廓或格子阴影，优先突出机器人本身；其他可移动格仍需有清楚的交互提示。这条不适用于可操作按钮的键盘焦点提示。
- 动效服务于移动、充能、答题、危险和胜负反馈；避免持续大幅晃动、频繁闪烁或遮挡关键操作。尊重 `prefers-reduced-motion`。
- 失败使用友好、非写实的爆炸或故障效果，配合“再接再厉”等鼓励文案及明确的重试操作。

## PC 与 iPad：横竖屏自适应、单屏无滚动

这是所有可游玩小游戏页面的硬性要求，包含正常游玩、挑战、帮助、设置、暂停及胜负结算状态。

1. **使用浏览器可用视口，而非浏览器全屏。** 不提供“进入/退出全屏”按钮，不自动调用 `requestFullscreen`、WebKit 全屏接口或锁定屏幕方向；不得要求玩家先添加到主屏幕才能正常玩。
2. **横屏、竖屏都能直接玩。** 宽屏通常将状态与操作区放右侧，窄屏或竖屏优先放底部；断点同时考虑宽度、高度和内容实际空间，不能只按设备名称或固定宽度判断。
3. **不出现页面或游戏面板的横向、纵向滚动条。** 棋盘、当前目标、关键状态和必要操作必须同时完整可见。不能只用 `overflow: hidden` 把溢出的内容裁掉，或者隐藏滚动条来假装适配完成。
4. **优先放大游戏区。** 压缩页头、面包屑、说明、边距和装饰，次要信息放入帮助或折叠区域；不要让角色展示卡、大块留白占据本可用于棋盘的空间。
5. **根据真实容器宽高计算尺寸。** 网格单元取可用宽度、可用高度能容纳的共同尺寸，包含行列数、间距及内边距；保持方格比例，不拉伸角色。不要用固定格子尺寸或任意尺寸上限限制大屏可用面积。
6. **采用稳定的尺寸更新机制。** 复用 `src/immersive.css` 的布局边界；使用 `100dvh`（保留必要回退）、`min-width: 0`、`min-height: 0` 和安全区内边距。需计算尺寸时使用容器 `ResizeObserver`，不要仅监听窗口宽度。
7. **旋转和缩放只改变布局。** 不能重开关卡、改变随机地图、丢失位置/能量/成绩或取消正在进行的挑战。切换难度时先计算新布局，避免旧格子尺寸短暂撑破新地图。
8. **弹窗也必须完整可用。** 标题、题目、答案、放弃/确认/重试等操作均在当前视口内；矮屏可重排按钮、精简装饰。长帮助可分页，不依靠弹窗滚动或把全文缩成难以阅读的小字。
9. **限制样式作用域。** 游戏页可锁定页面滚动，离开游戏页必须恢复正常文档行为。大厅、成就等内容页允许自然纵向滚动，但不得横向溢出；不要对整个站点永久设置滚动锁定。

## 字号与触控

- 当前能量、生命、得分、步数、战胜怪兽数量等关键状态必须明显可读；不能把重要信息当成小号装饰文字。
- PC/iPad 常规布局以关键数值约 28–36px、状态标签约 14–16px 为目标；窄屏可适度调整，但仍须保留清晰的数值与含义，不能重叠、截断或把一条能量状态挤成多行。
- 优先扩大游戏区和重新排版，最后才缩小文字；不要通过整体 `transform: scale(...)` 让按钮和关键字号一起缩水。
- 主要按钮、方向键和答案选项以至少 44×44 CSS px 的触控区域为目标。密集网格若受尺寸限制，必须提供足够大的替代操作控件。
- 同时支持鼠标与触屏；移动类游戏按需支持方向键/WASD。不可依赖 hover、右键或只能用键盘完成的操作。
- 图标按钮必须有清晰的可访问名称；保留键盘焦点和弹窗焦点管理。弹窗打开时阻止底层游戏响应移动输入。

## 交互、状态与音效

- 游戏规则放在独立的 TypeScript 模块中，界面负责展示和输入映射。地图、位置、能量与进度应来自同一个明确的游戏状态。
- 挑战需要有清楚的结果与退出策略。参考首个游戏：答对才进入怪兽格；放弃扣 1 点能量，留在原格，怪兽仍存在，不增加探索步数或战胜数量。其他游戏有不同代价时必须明确展示。
- 统一处理答对、答错、放弃、耗尽能量和重试，防止快速点击重复扣费、重复奖励、跳过挑战或重复触发结算。
- 首个游戏中，同一格的能量效果只生效一次；放弃挑战不能再次领取原格补给。后续修改不能破坏这些规则。
- 音效应清晰可闻，移动、充能、挑战和结算之间保持一致的响度；加强音效时避免突兀的尖锐声、叠加过载和失真。
- 保留易找到的静音入口并保存偏好。音频在用户交互后初始化；声音不可用时游戏仍可完整游玩。不要把判断成功或失败的必要信息只放在声音中。
- 胜利有明确的视觉庆祝与欢呼；失败有原因、鼓励和重试。不要为了增加刺激擅自加入倒计时或惩罚性文案。

## 实现与资源约定

- 新游戏沿用现有页面注册与 Hash 路由方式，共享组件优先；游戏专用样式限定作用域，避免影响大厅和其他游戏。
- 保持现有 GitHub Pages 子路径部署兼容性。资源、Manifest 和页面入口使用可部署的相对路径，不引入开发机绝对路径。
- 插画、音效等资源应可追踪来源，优先使用项目已有或本地资源；关键渲染不要依赖外部字体或图片网络请求成功。
- 定时器、事件监听、音频节点、尺寸观察器需要正确清理。弹窗、暂停与页面切换不能留下失效回调或重复输入处理。
- 不因新增一个小游戏就替换平台技术栈或大规模重写共享布局。

## 验收要求

修改游戏规则或界面后，按改动范围运行 `npm test`、`npm run build` 和相关 Playwright 测试。纯文档改动只需检查文档，不要求启动游戏或重跑全部测试。

- **浏览器：** PC Chromium 和 iPad WebKit 都要覆盖；保留已有手机触屏回归。模拟器测试不能描述成 iPad 真机已验证。
- **尺寸：** 至少检查 iPad 的 1024×768、768×1024、1180×720、820×1180；PC 的 1440×1000、1366×650。保留 393×852、320×568、852×393 等窄屏/矮屏用例。
- **地图：** 每个尺寸检查所有地图大小和难度，包括最大网格，不只验证默认关卡。
- **流程：** 覆盖移动、挑战、答对、答错、放弃、重试、胜利、帮助和设置；旋转前后确认当前局状态未被重置。
- **几何检查：** 页面与必要面板无溢出；所有格子、能量文字、状态数值、方向键及弹窗操作都在可用区域内，相互不遮挡。仅检查根节点无滚动条不算通过。
- **视觉检查：** 截图检查横屏、竖屏、最大地图和挑战弹窗，确认角色可辨认、当前机器人突出、文字清晰、棋盘足够大。自动断言不能替代截图检查。
- **测试隔离：** 沿用 `playwright.config.ts` 的独立生产预览服务，避免开发服务热更新或截图文件写入导致测试页面意外重载。
- **报告：** 明确说明已执行的检查与尚未验证的部分，不把已有失败当成通过，不声称模拟验证等同于真机验证。
